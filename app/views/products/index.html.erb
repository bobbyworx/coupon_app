<h1>Products</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
  </tr>
  <%= link_to 'Add new product', new_product_path %>
  <% @products.each do |product| %>
    <tr>
      <td><%= product.title %></td>
      <td><%= product.description %></td>
    </tr>
  <% end %>
</table>
